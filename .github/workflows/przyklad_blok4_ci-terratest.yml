name: 'b4 CI — Terratest (integration tests)'

# ==================================================
# Terratest — testy integracyjne Go
#
# - Plan-only test: uruchamiany na każdym PR (bez Azure)
# - Full integration test: workflow_dispatch + po merge
# ==================================================

on:
  pull_request:
    branches: [main]
    paths:
      - 'przyklad_blok4/infra/**'
      - 'przyklad_blok4/test/**'
  push:
    branches: [main]
    paths:
      - 'przyklad_blok4/infra/**'
      - 'przyklad_blok4/test/**'
  workflow_dispatch:
    inputs:
      run_integration:
        description: 'Uruchom pełne testy integracyjne (deploy + verify + destroy)'
        required: false
        default: 'false'
        type: choice
        options:
          - 'false'
          - 'true'

permissions:
  id-token: write
  contents: read

env:
  GO_VERSION: '1.23'
  TF_VERSION: '1.11'
  TEST_DIR: ./przyklad_blok4/test

jobs:
  # ==========================================
  # Plan-only test — składnia + plan (backend azurerm)
  # ==========================================
  plan-test:
    name: '🧪 Terratest — Plan Only'
    runs-on: ubuntu-latest
    env:
      ARM_CLIENT_ID: ${{ secrets.ARM_CLIENT_ID }}
      ARM_SUBSCRIPTION_ID: ${{ secrets.ARM_SUBSCRIPTION_ID }}
      ARM_TENANT_ID: ${{ secrets.ARM_TENANT_ID }}
      ARM_USE_OIDC: 'true'
      ARM_USE_AZUREAD: 'true'
      ARM_USE_CLI: 'false'
    defaults:
      run:
        working-directory: ./przyklad_blok4/test
    steps:
      - uses: actions/checkout@v4

      - name: Azure Login (OIDC)
        if: env.ARM_CLIENT_ID != '' && env.ARM_TENANT_ID != '' && env.ARM_SUBSCRIPTION_ID != ''
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.ARM_CLIENT_ID }}
          tenant-id: ${{ secrets.ARM_TENANT_ID }}
          subscription-id: ${{ secrets.ARM_SUBSCRIPTION_ID }}

      - uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}
          cache-dependency-path: przyklad_blok4/test/go.mod

      - uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: ${{ env.TF_VERSION }}
          terraform_wrapper: false   # Terratest wymaga czystego binarki

      - name: Sync Go modules
        run: |
          go mod tidy
          go mod download

      - name: Auth Diagnostics (plan)
        run: |
          echo "ARM_CLIENT_ID set: $([[ -n \"$ARM_CLIENT_ID\" ]] && echo yes || echo no)"
          echo "ARM_TENANT_ID set: $([[ -n \"$ARM_TENANT_ID\" ]] && echo yes || echo no)"
          echo "ARM_SUBSCRIPTION_ID set: $([[ -n \"$ARM_SUBSCRIPTION_ID\" ]] && echo yes || echo no)"
          echo "ACTIONS_ID_TOKEN_REQUEST_URL set: $([[ -n \"$ACTIONS_ID_TOKEN_REQUEST_URL\" ]] && echo yes || echo no)"
          echo "ACTIONS_ID_TOKEN_REQUEST_TOKEN set: $([[ -n \"$ACTIONS_ID_TOKEN_REQUEST_TOKEN\" ]] && echo yes || echo no)"
          echo "ARM_USE_OIDC=$ARM_USE_OIDC ARM_USE_AZUREAD=$ARM_USE_AZUREAD ARM_USE_CLI=$ARM_USE_CLI"

      - name: Run Plan-Only Tests
        run: go test -v -timeout 10m -run TestTerraformPlanOnly ./...

      - name: Test Summary
        if: always()
        run: |
          echo "## 🧪 Terratest — Plan Only" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "Testy planu Terraform (azurerm backend)." >> $GITHUB_STEP_SUMMARY

  # ==========================================
  # Full integration test — wymaga Azure OIDC
  # ==========================================
  integration-test:
    name: '🔬 Terratest — Integration'
    runs-on: ubuntu-latest
    if: >-
      github.event_name == 'workflow_dispatch' && github.event.inputs.run_integration == 'true' ||
      github.event_name == 'push'
    needs: [plan-test]
    environment: test-terratest        # Osobne environment z OIDC credentials
    defaults:
      run:
        working-directory: ./przyklad_blok4/test
    env:
      ARM_CLIENT_ID: ${{ secrets.ARM_CLIENT_ID }}
      ARM_SUBSCRIPTION_ID: ${{ secrets.ARM_SUBSCRIPTION_ID }}
      ARM_TENANT_ID: ${{ secrets.ARM_TENANT_ID }}
      ARM_USE_OIDC: 'true'
      ARM_USE_AZUREAD: 'true'
      ARM_USE_CLI: 'false'
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}
          cache-dependency-path: przyklad_blok4/test/go.mod

      - uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: ${{ env.TF_VERSION }}
          terraform_wrapper: false

      - name: Azure Login (OIDC)
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.ARM_CLIENT_ID }}
          tenant-id: ${{ secrets.ARM_TENANT_ID }}
          subscription-id: ${{ secrets.ARM_SUBSCRIPTION_ID }}

      - name: Sync Go modules
        run: |
          go mod tidy
          go mod download

      - name: Auth Diagnostics (integration)
        run: |
          echo "ARM_CLIENT_ID set: $([[ -n \"$ARM_CLIENT_ID\" ]] && echo yes || echo no)"
          echo "ARM_TENANT_ID set: $([[ -n \"$ARM_TENANT_ID\" ]] && echo yes || echo no)"
          echo "ARM_SUBSCRIPTION_ID set: $([[ -n \"$ARM_SUBSCRIPTION_ID\" ]] && echo yes || echo no)"
          echo "ACTIONS_ID_TOKEN_REQUEST_URL set: $([[ -n \"$ACTIONS_ID_TOKEN_REQUEST_URL\" ]] && echo yes || echo no)"
          echo "ACTIONS_ID_TOKEN_REQUEST_TOKEN set: $([[ -n \"$ACTIONS_ID_TOKEN_REQUEST_TOKEN\" ]] && echo yes || echo no)"
          echo "ARM_USE_OIDC=$ARM_USE_OIDC ARM_USE_AZUREAD=$ARM_USE_AZUREAD ARM_USE_CLI=$ARM_USE_CLI"

      - name: Run Integration Tests
        run: go test -v -timeout 30m -run TestTerraformApplyAndVerify ./...

      - name: Test Summary
        if: always()
        run: |
          echo "## 🔬 Terratest — Integration" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "Pełne testy integracyjne: deploy → verify → destroy." >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "Environment: **test-terratest**" >> $GITHUB_STEP_SUMMARY
